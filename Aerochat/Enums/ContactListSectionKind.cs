namespace Aerochat.Enums
{
    /// <summary>Which home list section a row belongs to (controls icon size / layout setting).</summary>
    public enum ContactListSectionKind
    {
        Favorites,
        Conversations,
        Servers,
    }
}
